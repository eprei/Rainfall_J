# Level0 Walkthrough

## 1. Program Analysis

### Decompilation with GDB
```bash
gdb level0
(gdb) b *main
(gdb) run
(gdb) x/20i $eip
```

### Important Assembly Code
```nasm
0x8048ed4 <main+20>:	call   0x8049710 <atoi>    ; Converts argument to integer
0x8048ed9 <main+25>:	cmp    eax,0x1a7          ; Compares with 0x1a7 (423 in decimal)
0x8048ede <main+30>:	jne    0x8048f58 <main+152> ; Jumps if not equal
```

## 2. Vulnerability Analysis

1. The program takes a command line argument
2. It converts this argument to an integer using `atoi`
3. It compares the result with `0x1a7` (423 in decimal)
4. If the comparison is equal, the program continues execution
5. If the comparison is not equal, it jumps to another part of the code

## 3. Exploitation

### Value Conversion
```bash
python3 -c "print(int('0x1a7', 16))"
# Result: 423
```

### Exploit Execution
```bash
./level0 423
```

## 4. Flag Retrieval

Once we get a shell, we can read the flag:
```bash
cat /home/user/level1/.pass
```

## 5. Technical Explanation

- `atoi` : Converts a string to an integer
- `cmp eax,0x1a7` : Compares the converted value with 423 (0x1a7 in hexadecimal)
- `jne` : Jump if Not Equal - Jumps if values are not equal
- By passing 423 as an argument, we avoid the jump and get a shell

## 6. Exploitation Script

The Python script uses pwntools to:
1. Connect to the server
2. Send the argument 423
3. Get a shell
4. Read the flag

## 7. Key Points

- Understand hexadecimal conversion (0x1a7 = 423)
- Use GDB for analysis
- Understand assembly instructions (cmp, jne)
- Exploit the comparison condition to get a shell

